타일이 objName 하나 갖고 있고
타일을 전부 다 생성한 다음 objName을 가지고 오브젝트 생성

if (map[index]->objName != blank)
  CreateObj(map, index, name)

------------------------------------
obj = make_asdf

size = deployTable->GetSize()


for (int i = 0 to size.y i++)
{
    for (int j = curindex; size.x)
	info[j + i * 50].setObj(obj);
}



deployTable


vector pos
 

오브젝트 필수 속성

pick -> 이름
break -> 이름 체력
crop -> 이름 진행도 퀄리티

